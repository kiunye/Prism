# frozen_string_literal: true

module PrismWeb
  class Engine < ::Rails::Engine
    # Mountable engine. Install generator, routes, controllers, and
    # migrations arrive in later milestones per AGENTS.md §6.
  end
end